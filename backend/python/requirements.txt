# Requirements for Raspberry Pi sign detection server
# Install these packages without hash verification to avoid piwheels conflicts
# Use: pip install --no-cache-dir --upgrade -r requirements.txt

# Core web framework
flask
flask-cors

# Computer vision and ML
numpy
opencv-python
Pillow
ultralytics

# PyTorch (use piwheels or official PyTorch repo)
torch
torchvision

# Raspberry Pi Camera (install separately on Pi)
# picamera2
